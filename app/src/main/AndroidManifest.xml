<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.movie_tracker">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Movie Tracker"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/Theme.Thiviru_pp">
        <activity android:name=".screens.ImageActivity"></activity>
        <activity android:name=".screens.MovieOnline" />
        <activity
            android:name=".screens.AddEditMovie"
            android:screenOrientation="portrait" />
        <activity
            android:name=".screens.ViewMovies"
            android:screenOrientation="portrait" />
        <activity
            android:name=".screens.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>